const X: int = 0;

const add = fn (a: int, b: int) int {
    return a + b;
}

const main = fn () int {
    let x: int = 0;

    if (x < 5 || x > 5) { 
        let y: int = x;
        return y; 
    } else {
        return 1;
    }

    return X;
}
