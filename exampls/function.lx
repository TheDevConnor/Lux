const STRING: str = "Hello, World!";
const NUM: int = 3;

const Direction = enum {
    left, right, up, down
};

/*
const Point = struct {
    x: int, 
    y: int,

    init = fn (self *Point, x int, y int) void {
        self.x = x;
        self.y = y;
    }
};
*/

;; Example of a function declaration
const add = fn (a: int, b: int) *int {
    return a + b;
}

const main = fn () int {
    let x: int = 0;
    return 0;
}